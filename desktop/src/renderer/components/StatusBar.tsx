import React from 'react'

/**
 * Conversation controls now live inside InputBar so they follow its active tab.
 * Kept as a no-op export for legacy imports while callers migrate.
 */
export function StatusBar(): React.JSX.Element | null {
  return null
}
